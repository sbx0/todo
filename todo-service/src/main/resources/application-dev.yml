server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://wsl2.sbx0.cn:3306/todo
    username: root
    password: justfordev
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
      hbm2ddl:
        schema-generation:
          script:
            append: false
    properties:
      jakarta:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: ./todo-service/src/main/resources/db/auto/generate.sql
    open-in-view: false
  data:
    redis:
      host: wsl2.sbx0.cn
      port: 6379
      database: 0
      password: justfordev
      repositories:
        enabled: false
logging:
  file:
    name: ./logs/current.log
  logback:
    rollingpolicy:
      file-name-pattern: ./logs/current-%d{yyyy-MM-dd}.%i.log
      clean-history-on-start: false
      max-file-size: 1MB
      total-size-cap: 1GB
      max-history: 30
  level:
    root: info
    org.hibernate.orm.jdbc.bind: info
    org.springframework.security: info
jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub
weixin:
  app-id: 1
  app-secret: 2
  auth:
    token: 3
chatgpt:
  api-key: 4
